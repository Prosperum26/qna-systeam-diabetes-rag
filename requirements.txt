# Crawler
requests==2.31.0
beautifulsoup4==4.12.0

# Embedding (local, sentence-transformers)
sentence-transformers==2.2.0

# Vector store (local ChromaDB)
chromadb==0.4.0

# Utils (tùy chọn, nếu dùng .env)
python-dotenv==1.0.0
